import React from 'react'

const ResourceStretcher = () => {
  return (

    // here check the strecher model and assign data model realted page  
    // the page handle the mdoel all action, crate, update, delte, read
    // new model, not table
    // arrayElem handle unlimited array elements in form and view
    

    <div>ResourceStretcher</div>
  )
}

export default ResourceStretcher